Stufe 5 — `voyager-persistence`: Rekorde und Profile überleben einen Neustart. Ausführliches Epic-Ziel siehe [anforderungen-voyager-rebuild.md](anforderungen-voyager-rebuild.md#4-ausbaustufen-uebersicht).

| ID | Story | Akzeptanzkriterium (EARS) | Schnittstelle/API-Bezug | Priorität | Status |
|---|---|---|---|---|---|
| US-5.01 (E5.1) | Als Entwickler möchte ich alle neun Entity-Klassen und die einzelne Flyway-Baseline-Migration implementieren, damit das im Design-Dokument beschriebene Datenmodell vollständig, konsistent und ohne verlorene Updates existiert. | shall alle neun Tabellen (`player`, `player_statistics`, `cup`, `map`, `cup_session`, `race_session`, `race_result`, `cup_result`, `map_personal_best`) mit den spezifizierten Primärschlüsseln und Spalten in `V1__baseline.sql` existieren. shall `race_result` die Spalten `validation_state` und `plausibility_error` für jede Zeile ab Tag eins tragen (`validation_state = VALID` für alle v1-Zeilen). While `map_personal_best` nach `layout_version` skaliert ist, shall `map.layout_version` aus einem Inhalts-Hash der Kartenkonfiguration abgeleitet werden. shall `race_result` (und/oder `cup_result`) die `TuningConfig`-Revision/den Hash aus Stufe 3 bei jeder Zeile mitführen. shall auf `map_personal_best` sowohl ein eindeutiger Index `(map_id, layout_version, game_mode, player_id)` als auch ein Index `(map_id, layout_version, game_mode, best_time_ms)` existieren. shall jede Entity-Klasse `@Entity`, non-final, mit No-Arg-Konstruktor sein und keine Assoziation deklarieren. | `voyager-persistence.entity.*` (9 Klassen), `V1__baseline.sql` | Must | offen |
| US-5.02 (E5.2) | Als Entwickler möchte ich Repository-Ports in `voyager-api` definieren und sie in `voyager-persistence` ausschließlich mit `StatelessSession` implementieren, damit `voyager-race` gegen In-Memory-Fakes testen kann und nie ein Hibernate-Proxy in die Spiellogik leckt. | While `PlayerProfileStore`, `RaceArchive`, `CupArchive` und `LeaderboardQuery` in `voyager-api` liegen, shall sie als plain Interfaces (nicht `sealed`) reimplementierbar sein. shall `PersistenceProvider` `sealed … permits DefaultPersistenceProvider` sein, mit statischer `create(PersistenceSettings settings)`-Factory. shall jede Schreib-Methode der Ports `void` zurückgeben. While keine Methode der Implementierung eine stateful `Session` öffnet, shall dies durch eine ArchUnit-Regel geprüft werden (kein `SessionFactory.openSession`/`inSession`/`fromSession`/`createEntityManager`). | `voyager-api`: `PlayerProfileStore`, `RaceArchive`, `CupArchive`, `LeaderboardQuery`, `PersistenceProvider` | Must | offen |
| US-5.03 (E5.3) | Als Entwickler möchte ich jede Lese-Query als `@NamedQuery`-HQL-Constructor-Expression implementieren, die direkt `voyager-api`-Records erzeugt, damit Reads nie eine Entity materialisieren und keine `LazyInitializationException` entstehen kann. | shall Leaderboard-, Cup-Standings- und Profil-bei-Join-Queries ausschließlich Records zurückgeben, nie eine Entity. shall jeder HQL-String als `@NamedQuery` deklariert sein; ein Testcontainers-Test shall jede benannte Query gegen ein per Flyway migriertes Schema ausführen und ihr Nicht-Fehlschlagen beim `SessionFactory`-Bootstrap bestätigen. shall Cup-Standings als eine flache Query implementiert sein (kein N+1-in-N+1); die Gruppierung nach Etappe erfolgt in Java. shall Profil-bei-Join aus genau drei Queries in einer Transaktion bestehen (Identität+Statistik, letzte Ergebnisse, persönliche Bestzeiten), ohne kartesisches Produkt über verbundene Collections. | `voyager-persistence` `@NamedQuery` (u. a. `TOP_BY_MAP`), `voyager-api LeaderboardEntry` | Must | offen |
| US-5.04 (E5.4) | Als Entwickler möchte ich den atomaren `map_personal_best`-Upsert und den Ein-Transaktions-Abschluss eines Rennens (`race_session` schließen, `race_result`-Batch-Insert, Upsert der Bestzeiten, Statistik-Inkrement) implementieren, damit kein Lost-Update mehr entsteht, wenn zwei Spieler im selben Fenster finishen. | shall der `map_personal_best`-Upsert exakt die spezifizierte einzelne `INSERT ... ON DUPLICATE KEY UPDATE`-Anweisung (oder eine äquivalente Einzelanweisung) verwenden, ohne vorheriges Lesen. When ein Rennen mit N Finishern abgeschlossen wird, shall dies als eine einzige Transaktion committen; ein im Test erzwungener Fehler mitten in der Transaktion shall null Teil-Zeilen hinterlassen. While `hibernate.jdbc.batch_size=50` konfiguriert und `rewriteBatchedStatements=true` in der JDBC-URL gesetzt ist, shall ein Test belegen, dass N Inserts weniger Roundtrips als N erzeugen. When zwei simulierte Finisher in überlappenden Transaktionen um dieselbe Bestzeit konkurrieren, shall kein Update verloren gehen. | `voyager-persistence` `MapPersonalBestRepositoryImpl`, `race_result`-Batch-Insert | Must | offen |
| US-5.05 (E5.5) | Als Entwickler möchte ich die zwei benannten Executors, die begrenzte MPSC-Completion-Queue mit budgetiertem Drain zu Tick-Beginn sowie den Outbox-/Retry-Pfad implementieren, damit der Tick-Thread nie auf JDBC wartet und kein Datenverlust beim Neustart entsteht. | While `voyager-db-read` (fest, 4 Plattform-Threads) und `voyager-db-write` (ein Plattform-Thread) die einzigen genutzten Executors sind, shall `ForkJoinPool.commonPool()` nirgends vorkommen. When ein `submit()`-Aufruf am Rennende erfolgt, shall er sofort `void` zurückgeben; ein Lasttest mit 50 gleichzeitigen Completions shall belegen, dass das Tick-Budget nie überschritten wird (Backlog wächst, kein Tick fällt aus). shall ein fehlgeschlagenes `offer()` als Incident geloggt werden, ohne zu blockieren. shall `shutdown(Duration)` die Retry-Queue vor dem Schließen der `SessionFactory` vollständig abarbeiten; ein Test, der den Prozess mitten im Backlog stoppt, shall belegen, dass ein sauberer `shutdown(Duration)`-Aufruf keinen Datenverlust verursacht. | `voyager-db-read`/`voyager-db-write`-Executors, `PersistenceEvent`-Queue | Must | offen |
| US-5.06 (E5.6) | Als Spieler möchte ich beim Betreten der Lobby sofort spielen können, auch wenn mein Profil noch aus der Datenbank geladen wird, damit ich beim Join nie warte, selbst bei einer langsamen Datenbank. | While ein Profil noch als `Pending` markiert ist, shall der Spieler Standard-Kosmetik in der Lobby sehen; kein Codepfad shall auf `Loaded` warten. shall Systeme ausschließlich per `switch`-Pattern-Match über `ProfileSlot` (`Pending`/`Loaded`/`Failed`) entscheiden; keine Null-Profilreferenz shall im Tick-Pfad existieren. When eine künstlich verzögerte Datenbank simuliert wird, shall ein zeitgesteuerter Test belegen, dass der Lobby-Join dadurch nicht verzögert wird. | `voyager-api` `ProfileSlot` (`Pending`/`Loaded`/`Failed`) | Must | offen |
| US-5.07 (E5.7) | Als Entwickler möchte ich den HikariCP-Connection-Pool exakt nach den spezifizierten Werten konfigurieren, damit die Poolgröße die tatsächliche Last abbildet (eine Transaktion pro Rennabschluss) statt naiver Intuition. | shall `maximumPoolSize=10`, `minimumIdle=10`, `connectionTimeout=5000`, `maxLifetime=1500000`, `keepaliveTime=120000` exakt wie dokumentiert gesetzt sein. While `leakDetectionThreshold` profilabhängig ist, shall es in Dev/Staging `10000` und in Produktion `0` betragen. shall `transactionIsolation=READ_COMMITTED` explizit gesetzt sein (nicht MariaDBs `REPEATABLE_READ`-Default); ein Test shall die dadurch vermiedene Gap-Lock-Serialisierung demonstrieren oder dokumentieren, warum dies in einem Unit-/Integrationstest nicht demonstrierbar war. shall die JDBC-URL `cachePrepStmts=true`, `useServerPrepStmts=true`, `prepStmtCacheSize=250`, `connectionTimeZone=UTC` und `forceConnectionTimeZoneToSession=true` enthalten. | HikariCP-Konfiguration, JDBC-URL-Parameter | Should | offen |
| US-5.08 (E5.8) | Als Entwickler möchte ich die eine höchstwertige Testcontainers-Integrationssuite bauen (MariaDB-Testcontainer → Flyway `migrate()` → `SessionFactory` im `validate`-Modus → jede benannte Query ausgeführt), damit Entity-/Schema-Drift, fehlerhafte HQL und fehlende Indizes vor grüner CI auffallen, nicht danach. | shall eine Testklasse den vollständigen Pfad Testcontainers-MariaDB → `migrate()` → `SessionFactory` (`hbm2ddl.auto=validate`) → Ausführung jeder `@NamedQuery` mindestens einmal abdecken. When während der Ticket-Bearbeitung testweise ein Entity-Feld absichtlich gebrochen wird, shall der Test fehlschlagen (Selbstcheck, nicht Teil der ausgelieferten Suite). shall die `EXPLAIN`-Assertions für die vier kritischen Queries (Leaderboard, Cup-Standings, Profil-bei-Join, Map-Record) Teil dieser Suite sein. shall die Suite in CI bei jedem PR laufen, der `voyager-persistence` berührt. | `voyager-persistence` Testcontainers-Integrationssuite | Must | offen |
| US-5.09 (E5.9) | Als Entwickler möchte ich `SessionFactoryCreator` und `PersistenceClock` als injizierbare `@FunctionalInterface`-Typen implementieren, damit Tests eine Testcontainers-Factory bzw. einen festen Zeitpunkt substituieren können, statt `LocalDateTime.now()` verstreut im Code zu nutzen. | While kein Aufruf von `LocalDateTime.now()`/`Instant.now()` außerhalb der `PersistenceClock`-Standardimplementierung existiert, shall dies durch eine ArchUnit-Regel geprüft werden. When ein Test einen festen `PersistenceClock` substituiert, shall ein deterministischer Zeitstempel in einer geschriebenen Entity nachweisbar sein. shall `SessionFactoryCreator` als Substitutionspunkt der E5.8-Testcontainers-Suite verwendet werden. | `voyager-persistence`: `SessionFactoryCreator`, `PersistenceClock` | Should | offen |
| US-5.10 (E5.10) | Als Entwickler möchte ich Regel 8 (`*Adapter`-Namenskonvention) um Row-to-Record-Adapter in `..persistence.adapter` erweitert sehen. **Won't — Begründung:** Laut `STATUS.md` ist dieses Ticket vermutlich redundant. E1.5 schreibt `CLAUDE.md` im Rahmen von Entscheidung D12 bereits vollständig neu und ist damit die maßgebliche Stelle für Konventionsänderungen wie diese `*Adapter`-Erweiterung; ein separates, nachgelagertes Amendment-Ticket würde diese Autorität duplizieren. Sollte E1.5 die Erweiterung nicht mit abdecken, ist dies als Nachtrag zu E1.5 zu behandeln, nicht als eigenständige Story. | Entfällt (Won't) — falls doch benötigt: shall ein Ein-Zeilen-Amendment zu `CLAUDE.md` Regel 8 vor Merge explizit vom Projektinhaber genehmigt sein. | `CLAUDE.md` Regel 8 (bereits durch E1.5/D12 abgedeckt) | Won't | offen |
